// function getPINs(observed) {
//   const result = [''];
//   const observedArr = observed.split('');
//   observedArr.forEach((numStr) => {
//     const possibleDigits = possibilities[Number(numStr)];
//       let newnewSet = [];
//     possibleDigits.forEach((digit) => {
//       const newSet = [...result];
//       newSet.forEach(comb => newnewSet.push(comb + digit);
//       result.concat(newnewSet);
//     });
//   });
//   return result;
// }